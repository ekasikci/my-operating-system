In order to run the OS “make run” command must be executed in the folder where the makefile is located. 

The OS name must be the same with the name in the makefile which is “Ertugrul’s Operating System”.

The output mykernel.iso file must be added to OS by using virtual box. In the first it is possible to encounter an error. After adding resulting iso file, the error will be resolved.

